package net.nawaman.javalanguagemodel.api.expression;

/**
 * Represents the creation of an array. This might have methods to get the element type, the size of the array, 
 *   and the initial values (which would be a list of Expression instances).
 */
public interface ArrayCreationExpression {
    
}
