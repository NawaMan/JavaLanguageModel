package net.nawaman.javalanguagemodel.api.expression;

/**
 * Represents object creation through a constructor.
 * This might have methods to get the type being constructed and the arguments to the constructor 
 *     (which would be a list of Expression instances).
 */
public interface ObjectCreationExpression {
    
}
