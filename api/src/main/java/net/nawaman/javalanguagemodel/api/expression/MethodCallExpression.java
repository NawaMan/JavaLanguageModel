package net.nawaman.javalanguagemodel.api.expression;

/**
 * Represents a method call. This might have methods to get the target object (which could be another Expression), 
 *   the method name, and the arguments (which would be a list of Expression instances).
 */
public interface MethodCallExpression {
    
}
