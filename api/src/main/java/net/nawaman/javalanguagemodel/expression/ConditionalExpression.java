package net.nawaman.javalanguagemodel.expression;

/**
 * Represents a conditional expression (ternary operator ? :).
 * This might have methods to get the condition, the true expression, and the false expression.
 */
public interface ConditionalExpression {
    
}
